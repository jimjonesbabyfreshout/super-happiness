name: Continuous Deployment

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set Up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: 'npm'
<<<<<<< main
      - run: npm install
      - run: npm run build
      - uses: jimjonesbabyfreshout/github-pages-deploy-action@4.3.3
=======

      - name: Install Dependencies
        run: npm install

      - name: Build Project
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@4
>>>>>>> origin/main
        with:
          branch: gh-pages
          folder: dist
